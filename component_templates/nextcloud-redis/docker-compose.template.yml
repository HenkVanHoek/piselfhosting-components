services:
  nextcloud-redis:
    image: redis:alpine
    container_name: nextcloud-redis
    restart: always
    labels:
      - "com.centurylinklabs.watchtower.enable=false"
    environment:
      TZ: "{{ TZ }}"
    networks:
      - nextcloud-internal
